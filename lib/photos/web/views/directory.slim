h1= @directory[1].to_s

- @photos.each do |photo|
  div
    figure
      img src=photo.sample('200x200').relative_path('/photos')
      figcaption
        = photo.basename
    div
      - [ '1080x768', 'Originals' ].each do |size|
        ' &nbsp;
        a href=photo.sample(size, generate: false).relative_path('/photos')
          = size
